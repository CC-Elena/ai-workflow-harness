# AI Workflow Harness 产品说明

## 1. 产品定位

AI Workflow Harness 是一套沉淀在代码库内的 AI 研发自动化工作流

它的核心定位是：以代码仓库为载体，把 PRD 解析、结构化 Spec、任务拆解、上下文召回、代码生成、测试校验、视觉校验、RCA 复盘和 Skills 迭代串成一套可版本化、可审查、可持续演进的研发闭环。

一句话概括：

> 基于仓库内的 Spec、Skills、Prompt、上下文索引和评估记录，让 AI 从单点编码助手升级为参与完整研发流程的执行框架。

## 2. 背景与问题

当前团队已经在使用 Cursor、Copilot、ChatGPT 等 AI 工具辅助开发，但这些工具主要解决的是局部编码效率问题，无法系统性解决从需求到交付的完整链路问题。

传统研发模式中主要存在以下痛点：

1. AI 参与度碎片化
   AI 主要介入代码编写环节，需求解析、技术方案、任务拆解、测试校验、代码审查和上线复盘仍依赖人工串联，导致 AI 的价值停留在个人效率工具层面。

2. 人工对齐成本高
   PRD 到代码实现之间存在多次人工转译，产品、研发、测试对需求边界、交互细节、验收标准的理解容易不一致，产生大量沟通和返工成本。

3. 企业规范难以注入
   通用 AI 工具不了解企业内部组件库、Hooks 规范、接口约定、权限规则、目录结构、历史业务经验和代码风格，容易生成“能跑但不合规”的代码。

4. 长链路任务不可控
   直接让 AI 从 PRD 生成完整代码，容易出现上下文缺失、任务跑偏、幻觉、重复修改、偏差累积等问题。

5. 经验无法沉淀和复用
   团队的工程经验通常散落在文档、代码评审、个人经验和即时沟通中，AI 无法稳定读取，也无法基于执行结果持续优化。

## 3. 建设目标

本项目希望将研发模式从“人工主导编码，AI 局部辅助”升级为“人工定义目标与监督逻辑，AI 按规范执行研发任务”。

核心目标包括：

1. 建立 Spec-driven 研发模式，将结构化 Spec 作为需求到代码的唯一可信源。
2. 建立仓库级项目语义地图，让 AI 精准理解项目结构、组件关系和调用链路。
3. 建立 Planner-Executor 工作流，降低长链路任务的幻觉和偏差累积。
4. 将项目工程规范沉淀为可执行 Skills，约束 AI 输出符合存量资产和团队规范。
5. 后续阶段可引入 MCP 服务连接企业知识库，为 AI 提供可控、可追踪的企业上下文；当前阶段明确排除 MCP 实现。
6. 建立测试、视觉校验、RCA 和效果评估闭环，让工作流可持续优化。

## 4. 使用形态

该工作流以代码仓库为核心载体，所有关键资产都随业务代码版本化。

推荐目录形态如下：

```text
repo/
├── specs/                     # PRD 转换后的结构化规格
├── .ai/
│   ├── workflows/             # Planner-Executor 工作流定义
│   ├── prompts/               # PRD 解析、任务拆解、代码生成、Review Prompt
│   ├── skills/                # Agent 可执行技能
│   ├── context/               # 项目语义地图、组件索引、Hook 索引
│   └── evals/                 # 执行效果评估、RCA 和回滚记录
├── mcp/                       # Future：企业知识库 MCP 接入，当前阶段不实现
├── docs/
│   └── engineering-rules/     # 工程规范、架构约束、组件使用准则
└── scripts/
    ├── generate-spec          # PRD 到 Spec
    ├── build-context          # 生成项目语义地图
    ├── run-workflow           # 执行 AI 工作流
    ├── run-eval               # 记录执行效果
    └── rca                    # 失败归因与优化建议
```

用户不需要进入一个独立平台操作，而是在仓库内通过脚本、CLI、Agent 会话和文档资产完成研发自动化。

## 5. 核心流程

完整工作流如下：

```text
PRD / 需求描述
   ↓
需求解析
   ↓
OpenSpec 结构化 Spec
   ↓
企业知识库召回
   ↓
项目上下文召回
   ↓
Planner 任务拆解
   ↓
Executor 分任务执行
   ↓
测试 / Lint / 类型检查 / 构建
   ↓
视觉校验 / UI 自愈
   ↓
RCA 失败归因
   ↓
人工 Review / 合并
   ↓
执行效果记录 / Skills 迭代
```

该流程的关键是让每个环节都有明确输入、输出、验收标准和失败回退机制，避免 AI 在长链路任务中自由发挥。

## 6. 核心模块

### 6.1 Spec-driven 研发模式

Spec-driven 的核心是把结构化 Spec 作为研发流程的唯一可信源。

PRD 通常是自然语言，容易存在歧义。工作流会将 PRD 转换为结构化 Spec，明确描述业务目标、功能范围、接口约束、组件要求、权限规则、异常场景、验收标准和设计稿约束。

Spec 的作用包括：

1. 产品、研发、测试的需求对齐依据。
2. Planner 拆解任务的输入。
3. Executor 生成代码的约束。
4. 测试和视觉校验的验收标准。
5. RCA 分析需求偏差的依据。

示例：

```yaml
feature: coupon-batch-send
goal: 支持运营批量发放优惠券
modules:
  - coupon-management
  - user-selection
  - batch-task
acceptance:
  - 支持按用户 ID 批量导入
  - 支持发放前校验库存
  - 支持失败用户导出
constraints:
  - 必须复用现有 Table 组件
  - 请求必须走统一 request 层
  - 不允许新增未经审批的依赖
```

### 6.2 MCP 企业知识库接入（Future）

当前阶段不实现 MCP，也不把 MCP 作为未完成项追踪。本节描述后续可扩展方向。

MCP 服务用于连接大模型和企业内部知识库，例如 Cooper。

它的作用不是简单搜索文档，而是为 Agent 提供安全、标准化、可追踪的企业上下文，包括：

1. 历史 PRD。
2. 历史技术方案。
3. 组件使用规范。
4. Hooks 使用规范。
5. 接口约定。
6. 错误码规范。
7. 权限规则。
8. 代码 Review 规范。
9. 同类需求落地案例。

通过 MCP 召回的内容会参与 Spec 生成、任务拆解、代码生成和 Review 校验，减少 AI 输出与企业规范脱节的问题。

### 6.3 项目语义地图

项目语义地图用于解决大模型上下文不完整或上下文过载的问题。

通过 Repomix 和静态分析能力，对代码仓库构建结构化索引，包括：

1. 目录结构。
2. 页面与路由。
3. 组件定义与调用关系。
4. 组件 props 与使用场景。
5. Hook 定义、入参出参和调用链路。
6. 接口调用关系。
7. 状态管理关系。
8. 样式规范。
9. 测试文件分布。

Agent 执行任务前，会基于任务内容从语义地图中精准召回相关上下文，而不是全量注入代码，从而降低 Token 成本并提高代码生成准确度。

### 6.4 上下文精准召回

上下文召回采用分级策略：

| 等级 | 内容 | 用途 |
|------|------|------|
| P0 | 当前任务直接修改文件 | 必须注入 |
| P1 | 直接依赖组件、Hook、接口 | 高优先级注入 |
| P2 | 同类历史实现 | 作为风格和模式参考 |
| P3 | 工程规范、业务规则 | 作为强约束 |
| P4 | 低相关背景信息 | Token 充足时补充 |

召回原则：

1. Spec 核心约束始终最高优先级。
2. 优先注入 P0 和 P1 上下文。
3. 低权重内容按 Token 预算裁剪。
4. 避免无关代码干扰模型判断。
5. 每次执行记录实际注入内容，方便 RCA 回溯。

### 6.5 Planner-Executor 工作流

Planner-Executor 架构用于降低长链路任务中的幻觉和偏差累积。

Planner 只负责规划：

1. 读取 Spec。
2. 结合项目语义地图判断影响范围。
3. 拆解原子任务。
4. 判断任务依赖关系。
5. 生成执行计划。
6. 为每个任务定义输入、输出和验收标准。

Executor 只负责执行：

1. 接收单个任务。
2. 读取任务相关上下文。
3. 调用对应 Skills。
4. 修改代码、补充测试和调整配置。
5. 输出变更说明、风险点和验证结果。

每个任务执行完成后必须进行校验，通过后才进入下一个任务；失败则触发 RCA 或重新执行。

### 6.6 Skills 体系

Skills 是 Agent 的可执行工程规范，本质是把团队经验、架构规则和最佳实践转成 AI 能稳定执行的指令集。

核心分类：

1. 规划类 Skills
   需求拆解、影响范围分析、技术方案生成、执行计划生成。

2. 开发类 Skills
   页面生成、组件复用、Hook 封装、接口对接、状态管理、样式调整。

3. 测试类 Skills
   单元测试生成、组件测试、接口 Mock、边界用例生成。

4. 校验类 Skills
   Lint 检查、类型检查、架构规范检查、依赖检查。

5. 自愈类 Skills
   报错分析、CI 修复、视觉偏差修复、失败任务重试。

6. RCA 类 Skills
   失败归因、人工修改分析、Prompt 优化建议、Skill 优化建议。

每个 Skill 应包含适用场景、输入要求、执行步骤、强制规范、输出格式、校验方式和常见失败原因。

### 6.7 视觉校验与自愈

针对前端项目，工作流需要支持视觉校验。

流程包括：

1. 从 Spec 中读取设计稿约束。
2. 启动本地页面。
3. 截取页面截图。
4. 与设计稿或基准图对比。
5. 识别布局、间距、字号、色值、组件样式偏差。
6. 定位相关代码。
7. 调用视觉修复 Skill。
8. 重新构建并再次截图校验。

视觉自愈优先解决明确、可量化、高频发生的 UI 偏差，例如间距错误、字号不一致、颜色 token 使用错误、表格列宽异常和移动端换行错位。

### 6.8 自动校验

Executor 完成任务后，必须执行自动校验。

校验项包括：

1. TypeScript 类型检查。
2. Lint 检查。
3. 单元测试。
4. 构建检查。
5. 依赖变更检查。
6. 目录结构检查。
7. 组件复用检查。
8. Spec 验收标准检查。
9. 视觉截图检查。

校验失败后，工作流需要记录失败原因，判断是否可自动修复；可修复则重新进入 Executor，不可修复则输出人工介入建议。

### 6.9 数字化评估与 RCA

每次 AI 执行都需要记录效果，用于持续优化。

核心指标：

1. 任务执行成功率。
2. 代码采纳率。
3. 人工修改率。
4. 回滚率。
5. Token 成本。
6. 执行耗时。
7. CI 失败率。
8. Review 问题数。
9. 视觉偏差修复次数。
10. Spec 缺陷数量。
11. Skill 命中率。
12. RCA 失败类型分布。

示例：

```yaml
run_id: ai-run-2026-001
feature: coupon-batch-send
success: true
token_cost: 148000
human_modification_rate: 12%
ci_passed: true
review_issues: 2
rca:
  - type: context_missing
    count: 1
  - type: style_inconsistent
    count: 1
```

RCA 分类示例：

1. Spec 缺失或表达不清。
2. 企业知识召回不足。
3. 项目上下文召回错误。
4. Skill 约束不完整。
5. 代码生成不符合架构。
6. 测试覆盖不足。
7. 视觉还原偏差。
8. 模型能力边界导致失败。

## 7. 角色与职责

### 7.1 研发工程师

1. 编写或审查 Spec。
2. 启动工作流。
3. 审查 AI 生成的代码。
4. 维护项目 Skills。
5. 根据 RCA 优化规则和上下文。

### 7.2 技术负责人

1. 制定架构约束。
2. 审查核心 Skills。
3. 评估 AI 研发效果。
4. 控制高风险场景的使用边界。

### 7.3 产品经理

1. 提供 PRD。
2. 协助确认结构化 Spec。
3. 明确验收标准和边界场景。

### 7.4 测试工程师

1. 审查测试用例。
2. 补充验收标准。
3. 参与失败归因与质量评估。

## 8. 非功能要求

### 8.1 可维护性

1. Spec、Prompt、Skills、工作流配置必须随仓库版本化。
2. 所有关键规则变更必须可 Code Review。
3. Skills 支持灰度更新和回滚。
4. Prompt 变更需要记录原因和效果。

### 8.2 安全性

1. Future MCP 访问企业知识库时需要权限控制；当前阶段不实现 MCP。
2. 不允许泄露敏感业务数据。
3. 外部工具调用需要记录日志。
4. AI 生成代码必须经过人工 Review 后才能合并。
5. 高风险核心链路不允许无人值守修改。

### 8.3 可观测性

1. 每次 Agent 执行必须有日志。
2. 每个任务需要记录输入、输出、上下文和结果。
3. 失败任务必须有 RCA 记录。
4. 支持按需求、Skill、失败类型追踪历史表现。

### 8.4 可扩展性

1. 支持新增 Skills。
2. Future 可支持新增 MCP 数据源；当前阶段不实现 MCP。
3. 支持接入不同模型。
4. 支持不同业务仓库按需配置自己的规则。
5. 支持前端、Node 服务、BFF 等不同工程类型扩展。

## 9. 成功指标

项目成功后，应达到以下指标：

1. 普通需求交付周期缩短 50% 以上。
2. 代码生成采纳率达到 70% 以上。
3. AI 生成代码人工修改率低于 30%。
4. 长链路任务执行成功率达到 80% 以上。
5. 代码 Review 返工率降低 50% 以上。
6. Token 成本相比全量上下文方案降低 50% 以上。
7. 新人理解项目结构时间缩短 50% 以上。
8. 常规前端页面需求可由 AI 完成大部分初稿实现。

## 10. 当前阶段边界

当前阶段不做以下事项：

1. 不做独立 SaaS 平台。
2. 不做完全无人值守上线。
3. 不绕过人工 Code Review。
4. 不自动修改高风险核心链路。
5. 不直接操作生产环境。
6. 不一次性覆盖所有业务仓库。
7. 不把所有项目规则硬编码进单个大 Prompt。

当前阶段重点是：

> 先在单个业务仓库中跑通 AI 研发闭环，让规则、上下文、Skills、校验和评估机制稳定沉淀，再逐步抽象为可复用模板。

## 11. 产品价值

### 11.1 对研发效率的价值

1. 降低需求拆解和代码初稿实现成本。
2. 减少重复 CRUD、样式调整、测试补全等低价值劳动。
3. 让研发从“写代码”转向“定义目标、审查逻辑、优化架构”。

### 11.2 对代码质量的价值

1. 通过 Spec 和 Skills 统一约束代码生成。
2. 通过自动校验减少低级错误。
3. 通过 RCA 持续优化失败模式。
4. 通过上下文召回提高与存量资产的一致性。

### 11.3 对团队沉淀的价值

1. 将隐性工程经验显性化。
2. 将散落文档转化为 Agent 可执行资产。
3. 降低新人理解项目和规范的成本。
4. 让团队的 AI 使用方式从个人经验升级为工程体系。
