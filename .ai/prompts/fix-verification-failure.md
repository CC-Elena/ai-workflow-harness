# Prompt: 修复验证失败

## 目标

当验证命令失败时，判断失败原因并进行最小修复。

## 执行步骤

1. 读取失败日志的关键错误。
2. 判断错误是否由本次修改引入。
3. 如果是本次修改引入，定位相关文件并最小修复。
4. 如果是既有问题，记录到 Run Record，不扩大修改范围。
5. 如果无法判断，向用户说明并停止。
6. 修复后重新运行失败的验证项。

## 输出要求

```text
失败项：
关键错误：
是否由本次修改引入：是 / 否 / 不确定
处理动作：
复验结果：
```

