# PRD: 失败路径与 RCA 样本

## 1. 背景

当前 Harness 已经有多个成功试点，但失败路径样本不足。评估框架要求在扩大推广前验证失败、阻断和 RCA 记录是否能被流程稳定承接。

## 2. 目标

构造一个不修改业务代码的失败样本，用于证明：

1. 验证失败不能被记录为成功。
2. Run Record 可以清楚描述失败现象。
3. RCA 可以追踪根因、修复动作和反哺项。
4. Harness Gate 能识别失败样本所需的 RCA 引用。

## 3. 非目标

1. 不制造真实代码缺陷。
2. 不让 CI 失败。
3. 不引入新的运行时依赖。
