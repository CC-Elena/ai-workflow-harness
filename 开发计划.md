
# AI Workflow Harness 开发计划

## 1. 开发原则

本项目不重复开发 Coding IDE 或 Agent 工具已经具备的能力。

Codex、Cursor、Claude Code 等工具已经具备代码读取、搜索、编辑、终端执行、测试运行、Git diff 理解、代码生成和代码 Review 等能力。本项目的建设重点不是重新实现这些能力，而是在代码库内沉淀一套让这些能力稳定发挥作用的研发工作流资产。

核心原则：

1. 复用 Codex 等 Coding IDE 的现有 Agent 能力。
2. 不自研 Agent Runtime。
3. 不自研代码编辑器。
4. 不自研模型调度系统。
5. 不做独立平台。
6. 优先沉淀仓库内的 Spec、Skills、Prompt、上下文索引、校验协议和 RCA 记录。

一句话目标：

> 在代码库内沉淀一套 Codex 可理解、可执行、可验证、可复盘的研发工作流资产，让现有 Coding IDE 的 Agent 能力在企业项目中稳定发挥作用。

## 2. 能力边界

### 2.1 复用 Coding IDE / Codex 的能力

以下能力由 Codex 等工具提供，本项目不重复开发：

1. 代码读取、搜索和理解。
2. 多文件代码编辑。
3. 终端命令执行。
4. Lint、Typecheck、Test、Build 调用。
5. Git diff 理解。
6. 代码 Review。
7. 基于上下文的代码生成。
8. 浏览器截图和页面验证能力。
9. 长任务执行中的人工协作。
10. 工具调用和文件修改执行。

### 2.2 本项目需要建设的能力

本项目只建设仓库内工作流资产：

1. 结构化 Spec 标准。
2. Planner-Executor 工作流协议。
3. 项目上下文索引。
4. Skills 使用路由。
5. Prompt 模板。
6. 校验协议。
7. 执行记录模板。
8. RCA 复盘模板。
9. 试点案例和团队使用指南。

## 3. 目标目录结构

第一阶段推荐形成以下目录结构：

```text
repo/
├── .ai/
│   ├── workflows/             # 工作流协议
│   ├── prompts/               # Prompt 模板
│   ├── context/               # 项目上下文索引
│   ├── evals/                 # 执行记录和 RCA
│   └── templates/             # Spec、Task、Run、RCA 模板
├── specs/                     # 具体需求 Spec
├── docs/
│   └── engineering-rules/     # 工程规范
└── skills/                    # 已有和新增 Skills
```


# PRD: Harness 下一阶段开发计划（跨工具可审计协议层）

> 本文件是给 AI Agent（Codex / Claude Code / Cursor / Windsurf / Trae 等）阅读的下一阶段产品需求文档。
> 阅读顺序：本 PRD → 后续 tasks.md → 对应阶段的 Skill / workflow。
> 修改本文件前必须先读 [AGENTS.md](../../AGENTS.md) 与 [.ai/workflows/command-routing.md](../../.ai/workflows/command-routing.md)。

## 1. 背景与判断

当前 AI Workflow Harness 已经具备一条可运行的仓库原生 AI 研发闭环：

1. **Lightweight 默认**：普通任务默认轻量执行，只有用户显式 `/spec` 或 `/mini-spec` 才进入对应 Spec 流程。
2. **Run Record + RCA**：交付过程可以记录输入、上下文、修改、验证、结论和失败复盘。
3. **Harness Gate**：`harness:check` 已能检查 Run Record 结构、验证证据、Skipped 原因和 PR diff 覆盖。
4. **Codex hooks**：已接入 SessionStart、UserPromptSubmit、PreToolUse、PostToolUse、Stop 等生命周期护栏。
5. **Skill 路由**：已有 `skills/*` 与 `.agents/skills/*` 适配，支持按需加载专项规则。

与 GitHub Spec Kit、Kiro、Claude Code Skills、Cursor Rules、Windsurf Rules 等主流 SDD / Harness 方案相比，当前项目的主线判断是：

- 不应复制 IDE 一体化体验，也不应自研 Agent Runtime。
- 不应让 AGENTS.md 变厚，也不应让所有任务默认进入完整 Spec。
- 应继续强化仓库原生协议层，让不同 Agent 都能读取同一套语义规则、验收标准、验证证据和失败复盘。

当前最需要补齐的短板有五类：

| 短板 | 影响 |
|------|------|
| 跨工具适配不足 | 当前偏 Codex-first，Claude Code / Cursor / Windsurf 等入口还缺少稳定映射 |
| 语义验收不足 | `harness:check` 能检查证据存在，但不能证明验收标准被覆盖 |
| Task verify 不可机读 | `tasks.md` 中的验证方式还不能稳定被脚本或 Agent 解析 |
| Skill 治理不够硬 | Skill frontmatter、触发条件、风险等级和加载预算还缺统一校验 |
| Harness 自身缺 Eval 回归 | 修改规则、hook 或模板后，缺少固定 fixture 判断行为是否变好 |

当前应继续巩固的护城河：

1. **仓库原生**：核心协议沉淀在 `.ai/`、`skills/`、`specs/`、`docs/` 和 `scripts/` 中。
2. **轻量默认**：小任务不被流程拖慢，复杂任务才升级。
3. **证据门禁**：成功、失败、跳过都需要真实证据或原因。
4. **失败复盘**：失败不是聊天记录里的偶发事件，而是能反哺规则资产的输入。

## 2. 北极星目标

下一阶段的目标是把 Harness 从：

```text
流程资产 + 证据门禁
```

升级为：

```text
跨工具可迁移、语义可验收、失败可学习的 AI 研发协议层
```

完成后，一个需求应能做到：

1. 不同 Agent 读取同一套仓库协议，不需要重写团队规则。
2. 每条验收标准都能追踪到任务、验证方式和证据。
3. 机械验证可以被脚本解析，语义验收不会被机械验证替代。
4. Skill 的触发、风险和加载预算可以被校验。
5. Harness 自身有 Eval fixture，规则升级后能做回归。
6. 验证失败、人工大幅修改或系统性 Review 问题能进入 RCA，并反哺 Prompt、Skill、模板、Hook 或检查脚本。

## 3. 非目标

下一阶段明确不做：

1. 不自研 Agent Runtime、调度器、编辑器、模型路由、数据库或 SaaS 平台。
2. 不开发 `specify` / `kiro` 类 CLI；脚本只服务校验、解析和 fixture 回归。
3. 不让 AGENTS.md 变厚；AGENTS.md 继续只做薄入口和路由。
4. 不让小任务默认进入 `/spec`；Lightweight Flow 仍是默认路径。
5. 不把 `tasks.md.verify` 机械验证等同于产品验收。
6. 不把 `allowed-tools` 作为 v1 强阻断来源，避免把仓库协议绑死在某个 Agent Runtime。
7. 不在跨工具适配、验收覆盖和 Eval 套件稳定前优先做模板分层。

## 4. 硬约束

1. 所有改动继续遵守 [AGENTS.md](../../AGENTS.md) 的 Lightweight 默认流程，除非用户显式触发 `/spec` 或 `/mini-spec`。
2. Harness 自身规则、模板、hook、脚本维护默认仍按 Lightweight Harness Maintenance 处理。
3. 所有新增检查不得破坏现有 `harness:check` 历史样本；若需要升级样本，必须说明迁移方式。
4. 新脚本优先扩展现有脚本；确需新增时，应保持小而可测。
5. 所有 PRD / Spec / Task / Run Record 模板变更必须能被 CI、fixture 或 `harness:check` 检查。
6. 验证失败不得写成通过；跳过验证必须写原因和风险。
7. 跨工具适配只能映射仓库协议，不得产生新的事实源。

## 5. 分阶段路线

每个阶段都应独立可发布、独立可回滚。阶段之间允许串行推进，但不要求一次性完成所有能力。

...（内容过长，已截断，建议分多次插入或替换）
