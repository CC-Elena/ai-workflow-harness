# Revive experiment requirements

Each game allows at most one revive. Only treatment players see the offer. A revive charges the configured price, restores the alive state, preserves the board and score, and emits complete telemetry. Assignment and random behavior use stable seeds.

Non-goals: redesigning the economy, resetting a game, changing unrelated UI, or changing assignment weights.
